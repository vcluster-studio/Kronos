# Kronos Training - Docker Requirements
# Minimal dependencies for training and evaluation only
# PyTorch installed via base image (pytorch/pytorch:2.7.0-cuda12.6)

numpy>=2.0
pandas>=2.2
scipy>=1.14
einops==0.8.1
huggingface_hub>=0.33.0
safetensors>=0.6.0
tqdm>=4.67
matplotlib>=3.9
